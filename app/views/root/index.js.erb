$(".job_posts").html("<%= escape_javascript(render(:partial => 'root/job_postings', locals: { job_postings: @job_postings })) %>");
$(".job_pagination").html("<%= escape_javascript( will_paginate @job_postings ) %>");
$('.job_pagination.add_remote a').attr('data-remote', 'true');
